import * as React from "react";
import Layout from "../components/layout";

const AboutPage = () => {
  return (
    <Layout pageTitle="Artificial Intelligence">
      <p>This is my experience working with AI projects.</p>
    </Layout>
  );
};

export const Head = () => <title>Artificial Inteligence</title>;

export default AboutPage;
